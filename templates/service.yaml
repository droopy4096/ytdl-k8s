apiVersion: v1
kind: Service
metadata:
  name: {{ include "youtube-dl-server.fullname" . }}
  labels:
    {{- include "youtube-dl-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "youtube-dl-server.selectorLabels" . | nindent 4 }}
